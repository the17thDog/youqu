const loginFn = require('./login')
const regFn = require('./reg')
const home_infoFn = require('./homeInfo')
const scenicFn = require('./scenic')
const userinfoFn = require('./userinfo')
const logoutFn = require('./logout')
const addMsgFn = require('./addMsg')
const likeFn = require('./like')
const adminFn  = require('./admin')
const searchFn = require('./search')
const commitFn = require('./commit')

module.exports = {
    loginFn,
    regFn,
    home_infoFn,
    scenicFn,
    userinfoFn,
    logoutFn,
    addMsgFn,
    likeFn,
    adminFn,
    searchFn,
    commitFn
}